<% flash.each do |name, msg| %>
    <div class="ui <%= name %> message transition">
        <i class="close icon"></i>
        <div class="header"></div>
        <p>
            <%= msg %>
        </p>
    </div>
<% end %>